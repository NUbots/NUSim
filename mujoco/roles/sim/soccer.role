# Full soccer simulation: physics + DDS bridge + locomotion + head camera +
# GameController supervisor + viewer (the window, or the browser viewer with
# --viser). ConsoleLog first so it captures startup logs.
k1sim_role(
    ConsoleLog
    Simulation
    SdkBridge
    Locomotion
    Camera
    Supervisor
    Viewer
    ViserViewer
)
